# Python's auto-generated cache — junk, never commit
__pycache__/

# Secrets and local config — must never reach the repo (Task 4!)
.env

# Local virtual environment folder, if we make one later
venv/
.venv/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

.pytest_cache/
.ruff_cache/

# Database backups — can contain real data, stay on the machine
backups/